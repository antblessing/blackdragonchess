<h1><%= @chess_class.school %> - <%= @chess_class.location %>,CA</h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @chess_class.size %>/60 students.</p>
     <p>$<%= @chess_class.price %> per quarter.</p>
     <p>Each <%= @chess_class.days %></p>
   </div>
   <% if current_user.admin? %>
   <div class="col-md-4">
     <%= link_to "Edit", edit_chess_class_path(@chess_class), class: 'btn btn-primary' %>
     <%= link_to "Delete", @chess_class, method: :delete, class: 'btn btn-danger', data: {confirm: "Are you sure?"} %>
   </div>
    <% else %>
     <%= link_to "Back", chess_classes_path, class: 'btn btn-primary' %>
    <% end %>
 </div>
 <br />
 <div class="row">
 Sign up for this class below:
 <%= render 'students/form', student: Student.new, chess_class: @chess_class %>
</div>
